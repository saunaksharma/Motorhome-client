import React from 'react'

// Small brand mark shown at the top of the admin (links to the dashboard).
export function Icon() {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/brand/emblem.png" alt="Motorhome Adventures" className="ma-icon" />
}
